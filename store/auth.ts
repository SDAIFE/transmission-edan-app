import { create } from 'zustand';

/**
 * Store Zustand pour l'authentification
 * 
 * TODO: Implémenter le store complet
 */

interface AuthState {
  // TODO: Définir l'état
}

export const useAuthStore = create<AuthState>()((set) => ({
  // TODO: Implémenter le store
}));

