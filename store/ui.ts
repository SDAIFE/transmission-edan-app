import { create } from 'zustand';

/**
 * Store Zustand pour l'UI
 * 
 * TODO: Implémenter le store pour gérer l'état UI
 */

interface UIState {
  // TODO: Définir l'état
}

export const useUIStore = create<UIState>()((set) => ({
  // TODO: Implémenter le store
}));

